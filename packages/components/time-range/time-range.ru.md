Меню выбора периода. Пользователь может выбрать одно из предустановленных значений или самостоятельно указать временной диапазон.

<!-- example(time-range-overview) -->

### Триггерный элемент

Компонент позволяет настроить триггерный элемент. Это может быть, например, кнопка, поле или псевдоссылка.

<!-- example(time-range-custom-trigger) -->

#### Триггер внутри поля

`kbq-time-range-title-as-control` помещает триггер внутрь `kbq-form-field`, поэтому у периода появляются
подпись, подсказка и стили ошибки обычного поля. `kbqTimeRangeTitlePlaceholder` оформляет текст,
который показывается при отсутствии значения, — чтобы это состояние вообще было достижимо, задайте
`[nonNullable]="false"`.

<!-- example(time-range-as-form-field) -->

### Пользовательские предустановки

Когда нет предустановленных значений, в окне остается только начало и конец периода.

<!-- example(time-range-empty-type-list) -->

### Ограничение выбора

Доступна настройка минимальной и максимальной даты. Например, диапазон с 2015 по 2017 год.

<!-- example(time-range-min-max) -->

### Настройка промежутков

Компонент позволяет задать варианты промежутков времени.

<!-- example(time-range-custom-range-types) -->

### Передача предустановок через DI

Список вариантов и сами варианты — это данные, поэтому заменить можно и то, и другое, не трогая шаблон:

- `KBQ_DEFAULT_TIME_RANGE_TYPES` заменяет список вариантов по умолчанию (`defaultTimeRangeTypes`) для
  всех `kbq-time-range` ниже провайдера. `[availableTimeRangeTypes]` переопределяет его для конкретного
  компонента.
- `KBQ_CUSTOM_TIME_RANGE_TYPES` регистрирует варианты, которых нет во встроенном списке. Каждая запись
  содержит идентификатор, длительность (`units`) и единицу, в которой форматируется подпись
  (`translationType`), либо фиксированный `range` вместо длительности.

```ts
providers: [
    {
        provide: KBQ_CUSTOM_TIME_RANGE_TYPES,
        useValue: [{ type: 'last3Weeks', units: { weeks: -3 }, translationType: 'weeks' }]
    },
    { provide: KBQ_DEFAULT_TIME_RANGE_TYPES, useValue: ['lastHour', 'last3Weeks', 'range'] }
];
```

### Пустое значение и коррекция значения

`nonNullable` (по умолчанию `true`) определяет, что означает `null`, записанный через форм-контрол.
Пока он `true`, `null` заменяется первым доступным вариантом и триггер всегда показывает период;
установите `false`, чтобы компонент мог оставаться без значения и показывать плейсхолдер.

`valueCorrected` срабатывает, когда компоненту пришлось изменить полученное значение: тип отсутствует
в `availableTimeRangeTypes`, у `range` нет одной из границ или пришел `null` при `nonNullable`.
Событие отдает исправленное значение, чтобы хост мог записать его обратно, и молчит, если значение уже
совпадает с тем, которое построил бы компонент.

### Свойства

| Имя                       | Тип                                                 | По умолчанию            | Описание                                                                         |
| ------------------------- | --------------------------------------------------- | ----------------------- | -------------------------------------------------------------------------------- |
| `minDate`                 | `T`                                                 | —                       | Самая ранняя дата, которую предлагают календари и принимает валидация.           |
| `maxDate`                 | `T`                                                 | —                       | Самая поздняя дата, которую предлагают календари и принимает валидация.          |
| `defaultRangeValue`       | `KbqRangeValue<T>`                                  | вчера → сегодня         | Начальные значения пары «от/до» для ручного диапазона.                           |
| `availableTimeRangeTypes` | `KbqTimeRangeType[]`                                | `defaultTimeRangeTypes` | Варианты в редакторе и их порядок. Первый выбран по умолчанию.                   |
| `showRangeAsDefault`      | `boolean`                                           | `true`                  | Оставляет ручной диапазон видимым, даже если `range` нет в списке вариантов.     |
| `nonNullable`             | `boolean`                                           | `true`                  | Подставляет значение по умолчанию вместо `null`.                                 |
| `arrow`                   | `boolean`                                           | `true`                  | Рисовать ли поповер со стрелкой.                                                 |
| `titleTemplate`           | `TemplateRef<KbqTimeRangeCustomizableTitleContext>` | —                       | Заменяет встроенный триггер.                                                     |
| `optionTemplate`          | `TemplateRef<KbqTimeRangeOptionContext>`            | —                       | Заменяет встроенное отображение варианта в редакторе.                            |
| `valueCorrected`          | `EventEmitter<KbqTimeRangeRange>`                   | —                       | Отдает исправленное значение, если переданное нельзя было использовать как есть. |
