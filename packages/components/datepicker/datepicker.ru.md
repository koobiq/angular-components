Поле с датой (дейтпикер) — специальное поле с виджетом для выбора даты.

<!-- example(datepicker-overview) -->

### Состояния

#### Неактивное поле

<!-- example(datepicker-inactive) -->

### Обязательно для заполнения

Если поле с датой обязательно для заполнения, используйте параметр required.

<!-- example(datepicker-required) -->

### Примеры использования

#### Ограничение на выбор даты

Диапазон задаётся двумя разными парами привязок, и нужны обе:

- `minDate` / `maxDate` на `<kbq-datepicker>` ограничивают календарь — дни вне диапазона
  выбрать нельзя.
- `min` / `max` на `<input>` включают валидацию, в том числе для даты, введённой с клавиатуры
  или вставленной из буфера. Значение вне диапазона всё равно попадает в контрол формы, а сам
  контрол помечается невалидным ошибкой `kbqDatepickerMin` или `kbqDatepickerMax`.

Валидация работает только при наличии контрола формы на поле ввода (`ngModel`, `formControl`
или `formControlName`) — без него валидаторы не вызываются.

`min` и `max` сравниваются вместе со временем суток, а не по календарным дням, потому что поле
ввода сохраняет время у разбираемого значения. Задавайте нижнюю границу началом первого
разрешённого дня, а верхнюю — концом последнего, чтобы оба граничных дня оставались валидными.

<!-- example(datepicker-minimax) -->

#### Диапазон дат

В Кубике нет специального виджета для выбора диапазона дат, поэтому используйте отдельные поля ввода, чтобы указать границы.

<!-- example(datepicker-range) -->

#### Использование Datepicker и Timepicker вместе

<!-- example(datepicker-and-timepicker) -->
