#### Список с ограничением по высоте

Элементы переносятся на новые строки и скрываются, когда выходят за пределы высоты контейнера,
в отличие от режима `nowrap`, где элементы выходят за пределы одной строки.

<!-- example(overflow-items-as-clamped-list) -->
