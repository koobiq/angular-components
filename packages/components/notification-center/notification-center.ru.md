Notification center — панель уведомлений о работе приложений

<!-- example(notification-center-overview) -->

Список уведомлений открывается из главного меню. В меню показывается счётчик непрочитанных сообщений: при 0 он скрыт, при количестве больше 99 отображается «99+».

Новые уведомления появляются в списке и группируются по датам, новые — сверху. Каждый элемент имеет заголовок и время, дополнительно может быть подпись и действия. По наведению или фокусу появляется кнопка удаления. Возможна очистка сообщений за день или удаление всех сразу.

В поле `date` можно передать любое значение, которое настроенный `DateAdapter` разбирает без явного формата — для адаптера Luxon это ISO 8601, RFC 2822 и SQL. От него зависят и группировка по дням, и порядок сортировки. Значение, которое адаптер разобрать не смог, показывается как есть в отдельной группе и сортируется последним, а не ломает список.

Непрочитанные сообщения помечены синей точкой, а для программ экранного доступа — подписью «Не прочитано». Сообщение помечается прочитанным, если задержать на нём курсор или клавиатурный фокус, а также при взаимодействии с элементом (ссылка, кнопка, удаление).

### Пустое состояние

При пустом состоянии показывается сообщение «Нет уведомлений», а кнопка «Удалить все» скрыта.

<!-- example(notification-center-empty) -->

### Ошибка

Если уведомления не удалось загрузить, вместо списка показывается сообщение «Не удалось загрузить уведомления» и кнопка «Повторить». Включите это состояние через `setErrorMode` и подпишитесь на `KbqNotificationCenterService.onReload`, чтобы повторить загрузку по нажатию кнопки.

<!-- example(notification-center-error) -->

### Бесконечная прокрутка

Список может догружать уведомления постранично по мере прокрутки к концу списка. Подпишитесь на `KbqNotificationCenterService.onNextPage`, чтобы загрузить следующую страницу, добавьте результат в `items` и управляйте процессом через `setLoadingMore`, `setHasMore` и `setLoadMoreErrorMode`. Порог, при котором начинается загрузка, настраивается свойством `scrolledToBottomOffset`.

<!-- example(notification-center-infinite-scroll) -->

### Удаление

Уведомления можно удалять по одному, группой за день или все сразу. Подпишитесь на `KbqNotificationCenterService.onDelete`, чтобы отреагировать на удаление — например, удалить элементы на сервере. Событие срабатывает во всех трёх случаях и содержит `type` (`'item'`, `'group'` или `'all'`) и `items` — удалённые уведомления. Удаление уведомления, которого нет в списке, ничего не делает и не порождает события, поэтому устаревшая ссылка не приведёт к лишнему удалению на сервере.

### Выпадающее окно

Центр уведомлений можно открыть в поповере. Например, при размещении в горизонтальном меню.

<!-- example(notification-center-popover) -->
