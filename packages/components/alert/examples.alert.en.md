### Dynamically shown alert

Insert the alert in response to a user action, mark it `role="alert"` so it is announced, dismiss it via the `(closed)` output, and return focus to the trigger.

<!-- example(alert-dynamic) -->
