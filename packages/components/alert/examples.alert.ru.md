### Динамически показываемый алерт

Вставляйте алерт в ответ на действие пользователя, помечайте его `role="alert"`, чтобы он озвучивался, скрывайте по событию `(closed)` и возвращайте фокус на кнопку-триггер.

<!-- example(alert-dynamic) -->
