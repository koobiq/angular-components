Компонент-заглушка для пустых состояний или сообщений об ошибке. Рамка не является частью компонента, а показана только для иллюстраций к статье.

<Example id="empty-state-overview" />

### Size

Для заглушек на весь экран можно использовать блок с элементами увеличенного размера

<Example id="empty-state-big" />

### Size - variations

Размер компонента устанавливается при помощи атрибута `size`.

<Example id="empty-state-size" />

### Align

Содержимое может быть выровнено по центру или по верхнему краю. Выравнивание по верхнему краю включается атрибутом `alignTop`.

<Example id="empty-state-align" />

У компонента нет собственной высоты: как flex-элемент он растягивается, в остальных случаях занимает высоту содержимого. Тогда оба выравнивания выглядят одинаково. Чтобы выравнивание действовало, задайте высоту компоненту или его контейнеру — в примерах для этого используется `min-height`.

### Error

Компонент Empty State можно использовать для показа сообщения об ошибке. Атрибут `errorColor` окрашивает заголовок и текст в красный, вместе с ними — иконку в слоте иллюстрации.

<Example id="empty-state-error" />

### Content

#### Иллюстрация

Размер иллюстрации можно менять. Иллюстрация большого размера подойдет для полноэкранных заглушек. Если нужна заглушка лишь для отдельного блока, то можно сделать иллюстрацию компактной или вовсе отказаться от нее.

<Example id="empty-state-content" />

В компоненте с центральным выравниванием и иллюстрацией появляется дополнительный нижний отступ, который поднимает композицию выше для оптической компенсации.

#### Иконка

В качестве иллюстрации можно вставить иконку

<Example id="empty-state-icon" />

#### Заголовок

<Example id="empty-state-title" />

#### Действия

Под текстом можно разместить одно или несколько действий: кнопки, ссылки и псевдоссылки.

<Example id="empty-state-actions" />

<Example id="empty-state-actions2" />

#### Только текст

<Example id="empty-state-text-only" />

#### Слоты под условием

Если слот отрисовывается по условию, это условие должно быть верхнего уровня:

```html
@if (failed()) {
<i kbq-empty-state-icon kbq-icon-item="kbq-triangle-exclamation_16"></i>
}
```

Вложенные друг в друга блоки и несколько элементов в одном блоке так не работают — из-за особенностей проекции содержимого в Angular. Такое содержимое не попадет в DOM, причем без ошибки и без предупреждения.

Если вариантов несколько, напишите для каждого свой `@if` на одном уровне: `@if (hasIcon() && wrapped())` и `@if (hasIcon() && !wrapped())`.

### Accessibility

Компонент задает заголовку типографику `subheading` для обычного размера и `headline` для большого. Размечайте заголовок тем уровнем, который нужен странице: `<h2 kbq-empty-state-title>`, а не `div`. Собственный отступ браузера компонент обнуляет, поэтому верстка не меняется.

Заглушка появляется на уже прочитанной странице: после поиска без результатов или после неудавшегося запроса. Добавьте на `<kbq-empty-state>` атрибут `role="status"`, чтобы вспомогательные технологии сообщили о ней, или `role="alert"` для состояния ошибки — о нем они сообщают настойчивее.

Компонент не проставляет `role` сам: заглушке, которая была на странице с самого начала (например, изначально пустой список), сообщать не о чем.
