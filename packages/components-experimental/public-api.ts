// This package has no public exports in v20.0.0. The only previously-shipped
// sub-package — `@koobiq/components-experimental/form-field` — graduated to the
// stable `@koobiq/components/form-field` and was removed.
//
// The package umbrella is intentionally kept (path mappings, tsconfig, ng-update
// packageGroup) so future experimental sub-packages can land without churn.
// Until then this file remains a deliberate empty barrel.
export {};
