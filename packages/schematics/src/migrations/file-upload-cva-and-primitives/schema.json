{
    "$schema": "http://json-schema.org/schema",
    "$id": "koobiq-components-file-upload-cva-and-primitives",
    "title": "Koobiq components file-upload review migration",
    "type": "object",
    "properties": {
        "project": {
            "type": "string",
            "description": "Name of the project to inspect. If omitted, the migration runs over the whole tree.",
            "$default": {
                "$source": "projectName"
            }
        }
    }
}
