{
    "$schema": "http://json-schema.org/schema",
    "$id": "koobiq-components-accordion-state-saving-default",
    "title": "Koobiq components accordion state saving default migration",
    "type": "object",
    "properties": {
        "project": {
            "type": "string",
            "description": "Name of the project to inspect. If omitted, the migration runs over the whole tree.",
            "$default": {
                "$source": "projectName"
            }
        }
    }
}
