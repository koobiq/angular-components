{
    "$schema": "http://json-schema.org/schema",
    "$id": "koobiq-components-username-pipe-injection",
    "title": "Koobiq components username review migration",
    "type": "object",
    "properties": {
        "project": {
            "type": "string",
            "description": "Name of the project to inspect. If omitted, the migration runs over the whole tree.",
            "$default": {
                "$source": "projectName"
            }
        }
    }
}
