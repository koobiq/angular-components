{
    "$schema": "http://json-schema.org/schema",
    "$id": "koobiq-components-input-number-surface",
    "title": "Koobiq components input review migration",
    "type": "object",
    "properties": {
        "project": {
            "type": "string",
            "description": "Name of the project to inspect. If omitted, the migration runs over the whole tree.",
            "$default": {
                "$source": "projectName"
            }
        }
    }
}
