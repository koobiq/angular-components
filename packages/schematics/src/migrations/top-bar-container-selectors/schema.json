{
    "$schema": "http://json-schema.org/schema",
    "$id": "koobiq-components-top-bar-container-selectors",
    "title": "Koobiq components top-bar container selectors migration",
    "type": "object",
    "properties": {
        "project": {
            "type": "string",
            "description": "Name of the project to migrate. If omitted, the migration runs over the whole tree.",
            "$default": {
                "$source": "projectName"
            }
        },
        "fix": {
            "type": "boolean",
            "default": true,
            "description": "When true, applies the renames. When false, prints what would change without writing."
        }
    }
}
