{
    "$schema": "http://json-schema.org/schema",
    "$id": "koobiq-components-code-block-optional-max-height",
    "title": "Koobiq components code block review migration",
    "type": "object",
    "properties": {
        "project": {
            "type": "string",
            "description": "Name of the project to inspect. If omitted, the migration runs over the whole tree.",
            "$default": {
                "$source": "projectName"
            }
        }
    }
}
