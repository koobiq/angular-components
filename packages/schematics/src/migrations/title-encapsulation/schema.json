{
    "$schema": "http://json-schema.org/schema",
    "$id": "koobiq-components-title-encapsulation",
    "title": "Koobiq components title review migration",
    "type": "object",
    "properties": {
        "project": {
            "type": "string",
            "description": "Name of the project to inspect. If omitted, the migration runs over the whole tree.",
            "$default": {
                "$source": "projectName"
            }
        }
    }
}
