{
    "$schema": "http://json-schema.org/schema",
    "$id": "koobiq-components-notification-center-signals",
    "title": "Koobiq components notification-center API report",
    "type": "object",
    "properties": {
        "project": {
            "type": "string",
            "description": "Name of the project to migrate. If omitted, the migration runs over the whole tree.",
            "$default": {
                "$source": "projectName"
            }
        }
    }
}
