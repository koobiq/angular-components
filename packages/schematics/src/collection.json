{
    "$schema": "../../../node_modules/@angular-devkit/schematics/collection-schema.json",
    "schematics": {
        "ng-add": {
            "description": "Adds @koobiq/components library to the project and installs dependencies",
            "factory": "./ng-add/index",
            "schema": "./ng-add/schema.json",
            "aliases": [
                "install"
            ]
        },
        "new-icons-pack": {
            "description": "Updates prefixes and names of changed/removed icons",
            "factory": "./migrations/new-icons-pack/index",
            "schema": "./migrations/new-icons-pack/schema.json"
        },
        "css-selectors": {
            "description": "Updates prefixes and names of changed/removed icons",
            "factory": "./migrations/css-selectors/index",
            "schema": "./migrations/css-selectors/schema.json"
        },
        "deprecated-icons": {
            "description": "Updates prefixes and names of changed/removed icons",
            "factory": "./migrations/deprecated-icons/index",
            "schema": "./migrations/deprecated-icons/schema.json"
        },
        "empty-state-size-attr": {
            "description": "Updates attr name and value in ts and html",
            "factory": "./migrations/empty-state-size-attr/index",
            "schema": "./migrations/empty-state-size-attr/schema.json"
        },
        "loader-overlay-size-attr": {
            "description": "Updates attr name and value in ts and html",
            "factory": "./migrations/loader-overlay-size-attr/index",
            "schema": "./migrations/loader-overlay-size-attr/schema.json"
        },
        "autocomplete-panel-width-auto": {
            "description": "Replaces kbq-autocomplete panelWidth=\"auto\" with panelWidth=\"fit-content\" in ts and html",
            "factory": "./migrations/autocomplete-panel-width-auto/index",
            "schema": "./migrations/autocomplete-panel-width-auto/schema.json"
        },
        "icons-replacement": {
            "description": "Updates names of icons in dedicated files",
            "factory": "./migrations/icons-replacement/index",
            "schema": "./migrations/icons-replacement/schema.json"
        },
        "v20-upgrade": {
            "description": "Migrates deprecated API removed in @koobiq/components v20.0.0",
            "factory": "./migrations/v20-upgrade/index",
            "schema": "./migrations/v20-upgrade/schema.json"
        },
        "filter-bar-signals": {
            "description": "Migrates KbqFilterBar consumers to the signal-based API (filter/pipeTemplates/isChanged/... reads become calls)",
            "factory": "./migrations/filter-bar-signals/index",
            "schema": "./migrations/filter-bar-signals/schema.json"
        },
        "alert-signals": {
            "description": "Migrates KbqAlert consumers to the signal-based API (compact/alertStyle reads become calls; warns on alertColor value change, now-protected members, narrowed inputs)",
            "factory": "./migrations/alert-signals/index",
            "schema": "./migrations/alert-signals/schema.json"
        },
        "dropdown-demote-overlay": {
            "description": "Removes the demoteOverlay input, the KBQ_DROPDOWN_HOST provider and dead .cdk-overlay-container_dropdown styles left by the overlay demotion removal",
            "factory": "./migrations/dropdown-demote-overlay/index",
            "schema": "./migrations/dropdown-demote-overlay/schema.json"
        },
        "app-switcher-signals": {
            "description": "Migrates KbqAppSwitcherTrigger consumers to its model()-based API (selectedApp reads become calls, writes become .set()) and warns on selectedSite value semantics, the removed header/footer/isTrapFocus/getIcon members and the new inline-icon sanitization",
            "factory": "./migrations/app-switcher-signals/index",
            "schema": "./migrations/app-switcher-signals/schema.json"
        },
        "button-state-and-styles": {
            "description": "Rewrites the removed physical border-radius mixins to logical ones and warns about the v20.3.0 button host-attribute, button-group ownership, .kbq-progress and locale changes",
            "factory": "./migrations/button-state-and-styles/index",
            "schema": "./migrations/button-state-and-styles/schema.json"
        },
        "form-field-signals": {
            "description": "Migrates KbqFormField and KbqHint consumers to the signal-based API (content-query and has* reads become calls, KbqPasswordHint.regex writes become .set()), moves [attr.aria-label] on kbq-cleaner to the [aria-label] input, and warns on the lost QueryList API, the removed mixinColor and the new required a11y locale keys",
            "factory": "./migrations/form-field-signals/index",
            "schema": "./migrations/form-field-signals/schema.json"
        },
        "button-truncation": {
            "description": "Moves edge icons of a button/button-toggle into the kbqButtonPrefix/kbqButtonSuffix slots so labels keep their ellipsis, and reports stylesheet overrides affected by the new truncation layout",
            "factory": "./migrations/button-truncation/index",
            "schema": "./migrations/button-truncation/schema.json"
        },
        "tag-slots": {
            "description": "Marks legacy implicit kbq-icon content in tags with kbqTagPrefix so tags can use explicit content slots",
            "factory": "./migrations/tag-slots/index",
            "schema": "./migrations/tag-slots/schema.json"
        },
        "button-supported-colors": {
            "description": "Removes button colors outside theme/theme-fade/contrast/contrast-fade from templates and warns about the ones it cannot resolve, plus the transparent default color change",
            "factory": "./migrations/button-supported-colors/index",
            "schema": "./migrations/button-supported-colors/schema.json"
        },
        "mandatory-peer-dependencies": {
            "description": "Adds @angular/animations, overlayscrollbars and @koobiq/date-adapter, which became mandatory peers of @koobiq/components, to projects whose package manager does not install new peers on upgrade",
            "factory": "./migrations/mandatory-peer-dependencies/index",
            "schema": "./migrations/mandatory-peer-dependencies/schema.json"
        },
        "button-toggle-signals-and-aria": {
            "description": "Migrates KbqButtonToggleGroup consumers to its signal inputs (vertical/multiple reads become calls) and reports the ARIA, tab-order and keyboard changes of the button-toggle review, including icon-only toggles left without an accessible name",
            "factory": "./migrations/button-toggle-signals-and-aria/index",
            "schema": "./migrations/button-toggle-signals-and-aria/schema.json"
        },
        "scrollbar-deprecated-path": {
            "description": "Rewrites @koobiq/components/scrollbar imports to @koobiq/components/scrollbar/deprecated — the overlayscrollbars-based implementation moved there when the dependency-free implementation was promoted to @koobiq/components/scrollbar",
            "factory": "./migrations/scrollbar-deprecated-path/index",
            "schema": "./migrations/scrollbar-deprecated-path/schema.json"
        },
        "filter-bar-rename-action": {
            "description": "Removes the `name` key from filter-bar locale configurations and reports the rename rework: renaming a filter no longer marks it saved, so it stops persisting the pending pipe edits",
            "factory": "./migrations/filter-bar-rename-action/index",
            "schema": "./migrations/filter-bar-rename-action/schema.json"
        },
        "navbar-signals-and-aria": {
            "description": "Migrates navbar consumers to the signal-based API (expanded/configuration/isCollapsed/collapsable reads become calls, expanded/tabIndex writes become .set(), rectangle-element horizontal/vertical become orientation) and reports the removed tooltip inheritance, the removed disabled/hovered members and the new ARIA, keyboard and z-index behaviour",
            "factory": "./migrations/navbar-signals-and-aria/index",
            "schema": "./migrations/navbar-signals-and-aria/schema.json"
        },
        "locale-configuration-providers": {
            "description": "Rewrites { provide: KBQ_<X>_CONFIGURATION, useValue: … } provider entries of the navbar, notification-center, app-switcher, search-expandable, datepicker and filter-bar to the kbq<X>LocaleConfigurationProvider() helpers, which is what still overrides the locale now that those tokens carry the defaults only, and warns on the provider shapes it cannot rewrite plus the removed externalConfiguration and read-only configuration members",
            "factory": "./migrations/locale-configuration-providers/index",
            "schema": "./migrations/locale-configuration-providers/schema.json"
        },
        "list-tree-multiple-input": {
            "description": "Rewrites the reworked multiple input of kbq-list-selection and kbq-tree-selection in ts and html",
            "factory": "./migrations/list-tree-multiple-input/index",
            "schema": "./migrations/list-tree-multiple-input/schema.json"
        },
        "title-encapsulation": {
            "description": "Reports the members of KbqTitleDirective that stopped being public in the title review — the removed resizeStream Subject (the directive now injects the CDK SharedResizeObserver and re-measures container resizes on its own, so a manual push has no counterpart), the now-private hasOnlyText, the removed ngOnDestroy declaration that a subclass calling super.ngOnDestroy() depended on, and the child/parent/isHorizontalOverflown/isVerticalOverflown/handleElementEnter/hideTooltip measurement members that became protected. Warn-only: each member is gone or hidden outright, so there is no expression to rewrite a call site to.",
            "factory": "./migrations/title-encapsulation/index",
            "schema": "./migrations/title-encapsulation/schema.json"
        },
        "file-upload-deprecated-outputs": {
            "description": "Renames the deprecated KbqMultipleFileUploadComponent.fileQueueChanged / KbqSingleFileUploadComponent.fileQueueChange outputs to filesChange / fileChange",
            "factory": "./migrations/file-upload-deprecated-outputs/index",
            "schema": "./migrations/file-upload-deprecated-outputs/schema.json"
        },
        "split-button-optional-disabled": {
            "description": "Reports the KbqSplitButton members whose type changed in the split-button review. disabled reports boolean | undefined instead of boolean: the backing field has no initializer, so a control with no [disabled] binding always returned undefined behind a non-nullable type, and the call sites that assigned it to a boolean or compared it against false were quietly wrong. The protected buttons content query moved from QueryList<KbqButton> to a signal query, so a subclass reading buttons.changes or buttons.length has to read buttons() instead. Warn-only: narrowing boolean | undefined back to boolean is a decision the call site owns. Notes the silent part too — an empty <kbq-split-button> no longer throws outside dev mode.",
            "factory": "./migrations/split-button-optional-disabled/index",
            "schema": "./migrations/split-button-optional-disabled/schema.json"
        },
        "popover-leave-delay": {
            "description": "Reports the popover call sites whose meaning changed in the popover review. A dead this.leaveDelay ?? 500 — the base initialises the field to 0, and 0 ?? 500 is 0 — made hover mode close the panel before the pointer could cross the gap to it. The delay is derived from the trigger now, and kbqLeaveDelay is a write-only input that records having been bound, so a programmatic leaveDelay assignment no longer sticks: the next write to trigger overwrites it. Also reports the now-readonly onConfirm and placementChange emitting KbqPopUpPlacementValues instead of string. Warn-only. Notes the parts with no call site: the confirm popover takes its defaults from the locale instead of hardcoded Russian, the untorn-down global ScrollDispatcher subscription is bounded, and the trigger can be imported standalone.",
            "factory": "./migrations/popover-leave-delay/index",
            "schema": "./migrations/popover-leave-delay/schema.json"
        },
        "tooltip-pointer-events-and-types": {
            "description": "Reports the tooltip review changes a consumer has to act on. The headline is silent: ignoreTooltipPointerEvents defaults to false instead of true, so every pane is hoverable (WCAG 1.4.13) and also captures clicks meant for whatever it floats over — markup that never mentioned the input compiles unchanged and behaves differently, so the migration reports the absence of the input rather than its presence. Also reports the removed KbqPopUpTrigger.scheduler, the delay argument getMouseLeaveListener() no longer takes, placementChange emitting KbqPopUpPlacementValues instead of string, and the any that left content/header/context/modifier. Warn-only: the markup whose behaviour changed is the markup that says nothing about the input, and a narrowed type is fixed by giving the call site the right type.",
            "factory": "./migrations/tooltip-pointer-events-and-types/index",
            "schema": "./migrations/tooltip-pointer-events-and-types/schema.json"
        },
        "toast-stack-and-defaults": {
            "description": "Reports the breaking parts of the toast review — the members KbqToastComponent lost when it moved from KbqToastService to the KBQ_TOAST_STACK token, the narrowed animation subject and showTemplate context — and notes that the data passed to show() is no longer written to, so its style and icon defaults are resolved inside the toast",
            "factory": "./migrations/toast-stack-and-defaults/index",
            "schema": "./migrations/toast-stack-and-defaults/schema.json"
        }
    }
}
